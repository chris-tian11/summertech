class Node:
    def __init__(self, data):
        self.data = data
        self.next_node = None
    def link(next_node1):
        self.next_node = next_node1
class list:
    def __init__(self, add_node):
        self.add_node = add_node
        head_node = None
        size = 0
    def add_node(self, data):
        node = Node(data)
        if size == 0:
            self.head_node = node
        else:
            current_node = self.head_node
            for i in range(size):
                self.next_node = node
            current_node = node
        size += 1